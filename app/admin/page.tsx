'use client'
import {useEffect,useState} from 'react'
import {supabase} from '../../lib/supabase'
export default function AdminPage(){const [allowed,setAllowed]=useState<boolean|null>(null),[name,setName]=useState('Administrador');useEffect(()=>{(async()=>{if(!supabase){setAllowed(false);return}const {data:{user}}=await supabase.auth.getUser();if(!user){setAllowed(false);return}const {data:p}=await supabase.from('profiles').select('role,full_name').eq('id',user.id).single();setAllowed(p?.role==='admin'||p?.role==='manager');if(p?.full_name)setName(p.full_name)})()},[]);async function logout(){if(supabase)await supabase.auth.signOut();window.location.href='/login'}if(allowed===null)return <main className="container"><div className="card">Verificando acesso...</div></main>;if(!allowed)return <main className="container"><section className="hero"><h1>Painel administrativo</h1><p>Área restrita do AGENDA-GO.</p></section><div className="card"><p>Você precisa entrar com uma conta de administrador ou gerente.</p><a className="btn" href="/login">Ir para o login</a></div></main>;return <main className="container"><section className="hero"><h1>Painel administrativo</h1><p>Bem-vindo, {name}. Gerencie a operação do AGENDA-GO por aqui.</p></section><section className="grid"><a className="card" href="/admin/nova-reserva" style={{textDecoration:'none',color:'inherit'}}><h3>Nova reserva</h3><div className="value">Reservar</div><p>Crie uma locação para um cliente.</p></a><a className="card" href="/admin/reservas" style={{textDecoration:'none',color:'inherit'}}><h3>Reservas</h3><div className="value">Gerenciar</div><p>Confirme, inicie, finalize ou cancele.</p></a><a className="card" href="/admin/veiculos" style={{textDecoration:'none',color:'inherit'}}><h3>Veículos</h3><div className="value">Frota</div><p>Cadastre e acompanhe os veículos.</p></a><a className="card" href="/admin/clientes" style={{textDecoration:'none',color:'inherit'}}><h3>Clientes</h3><div className="value">Cadastros</div><p>CPF, CNH, telefone, endereço e nascimento.</p></a><a className="card" href="/admin/pagamentos" style={{textDecoration:'none',color:'inherit'}}><h3>Pagamentos</h3><div className="value">Configurar</div><p>Ative PIX ou cartão e defina o acréscimo do cartão.</p></a></section><section className="section card"><div className="actions"><a className="btn" href="/admin/nova-reserva">+ Nova reserva</a><a className="btn secondary" href="/admin/clientes">Clientes</a><a className="btn secondary" href="/admin/reservas">Reservas</a><a className="btn secondary" href="/admin/veiculos">Veículos</a><a className="btn secondary" href="/admin/pagamentos">Pagamentos</a><button className="btn" onClick={logout}>Sair do administrador</button></div></section></main>
}