'use client'
import {FormEvent,useEffect,useState} from 'react'
import {supabase} from '../../../lib/supabase'
const types=[['cpf','CPF'],['cnpj','CNPJ'],['email','E-mail'],['phone','Telefone'],['random','Chave aleatória']]
export default function Recebimentos(){const [ok,setOk]=useState<boolean|null>(null),[companyId,setCompanyId]=useState(''),[company,setCompany]=useState<any>(null),[form,setForm]=useState({payout_pix_key:'',payout_pix_key_type:'cpf',payout_pix_holder_name:'',payout_pix_holder_document:'',payout_pix_enabled:false}),[msg,setMsg]=useState('');useEffect(()=>{(async()=>{if(!supabase){setOk(false);return}const {data:{user}}=await supabase.auth.getUser();if(!user){setOk(false);return}const {data:p}=await supabase.from('profiles').select('role,company_id').eq('id',user.id).single();const allowed=['admin','manager'].includes(p?.role||'')&&!!p?.company_id;setOk(allowed);if(!allowed)return;setCompanyId(p.company_id);const {data:c}=await supabase.from('companies').select('name,payout_pix_key,payout_pix_key_type,payout_pix_holder_name,payout_pix_holder_document,payout_pix_enabled').eq('id',p.company_id).single();setCompany(c);if(c)setForm({payout_pix_key:c.payout_pix_key||'',payout_pix_key_type:c.payout_pix_key_type||'cpf',payout_pix_holder_name:c.payout_pix_holder_name||'',payout_pix_holder_document:c.payout_pix_holder_document||'',payout_pix_enabled:!!c.payout_pix_enabled})})()},[]);function normalizeKey(){let key=form.payout_pix_key.trim();if(['cpf','cnpj','phone'].includes(form.payout_pix_key_type))key=key.replace(/\D/g,'');return key}async function save(e:FormEvent){e.preventDefault();if(!supabase||!companyId)return;const key=normalizeKey();if(form.payout_pix_enabled&&!key){setMsg('Informe a chave PIX antes de ativar os repasses.');return}if(form.payout_pix_key_type==='cpf'&&key&&key.length!==11){setMsg('Informe um CPF válido para a chave PIX.');return}if(form.payout_pix_key_type==='cnpj'&&key&&key.length!==14){setMsg('Informe um CNPJ válido para a chave PIX.');return}if(form.payout_pix_key_type==='email'&&key&&!/^\S+@\S+\.\S+$/.test(key)){setMsg('Informe um e-mail válido.');return}const payload={...form,payout_pix_key:key,payout_pix_updated_at:new Date().toISOString(),updated_at:new Date().toISOString()};const {error}=await supabase.from('companies').update(payload).eq('id',companyId);setMsg(error?error.message:'Dados de repasse PIX atualizados com sucesso.');if(!error)setForm({...form,payout_pix_key:key})}if(ok===null)return <main className="container"><div className="card">Carregando recebimentos...</div></main>;if(!ok)return <main className="container"><div className="card"><h1>Recebimentos</h1><p>Acesso restrito ao administrador da locadora.</p></div></main>;return <main className="container"><section className="hero"><h1>Recebimentos / Repasse PIX</h1><p>{company?.name||'Sua locadora'} pode informar a chave PIX para receber repasses do AGENDA-GO.</p></section><form className="card form" onSubmit={save}><h2>Conta de recebimento</h2><label>Tipo da chave PIX<select value={form.payout_pix_key_type} onChange={e=>setForm({...form,payout_pix_key_type:e.target.value})}>{types.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label>Chave PIX<input value={form.payout_pix_key} onChange={e=>setForm({...form,payout_pix_key:e.target.value})} placeholder="Informe a chave PIX"/></label><label>Nome do titular<input value={form.payout_pix_holder_name} onChange={e=>setForm({...form,payout_pix_holder_name:e.target.value})} placeholder="Nome do titular da chave"/></label><label>CPF/CNPJ do titular<input value={form.payout_pix_holder_document} onChange={e=>setForm({...form,payout_pix_holder_document:e.target.value})} placeholder="Documento do titular"/></label><label className="paymentOption selected"><span><input type="checkbox" checked={form.payout_pix_enabled} onChange={e=>setForm({...form,payout_pix_enabled:e.target.checked})}/> Ativar repasse PIX para esta locadora</span><small>As tarifas e comissões continuam definidas exclusivamente pelo Super Admin.</small></label><div className="card"><strong>Importante</strong><p>Esta chave indica onde a locadora deseja receber. O envio automático depende da habilitação da API Pix de pagamentos/repasses da conta Efí do AGENDA-GO e das regras comerciais e regulatórias aplicáveis.</p></div><button className="btn">Salvar dados de recebimento</button>{msg&&<div className="message">{msg}</div>}</form></main>