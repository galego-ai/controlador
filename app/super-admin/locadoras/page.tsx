'use client'
import {FormEvent,useEffect,useState} from 'react'
import {supabase} from '../../../lib/supabase'
const slugify=(v:string)=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')
const empty={name:'',slug:'',document:'',email:'',phone:'',plan_name:'Profissional',monthly_fee:0,primary_color:'#FFD400',secondary_color:'#111111',logo_url:'',pix_key:'',efi_payee_code:'',efi_account_number:'',efi_split_enabled:false,notes:''}
export default function Locadoras(){const [ok,setOk]=useState<boolean|null>(null),[list,setList]=useState<any[]>([]),[form,setForm]=useState<any>(empty),[editing,setEditing]=useState<string|null>(null),[msg,setMsg]=useState('');async function load(){if(!supabase)return;const {data}=await supabase.from('companies').select('*').order('created_at',{ascending:false});setList(data||[])}useEffect(()=>{(async()=>{if(!supabase){setOk(false);return}const {data:{user}}=await supabase.auth.getUser();if(!user){setOk(false);return}const {data:p}=await supabase.from('profiles').select('is_super_admin').eq('id',user.id).single();const allowed=!!p?.is_super_admin;setOk(allowed);if(allowed)await load()})()},[]);async function save(e:FormEvent){e.preventDefault();if(!supabase)return;const slug=form.slug||slugify(form.name);const payload={...form,slug,monthly_fee:Number(form.monthly_fee||0),updated_at:new Date().toISOString()};const q=editing?supabase.from('companies').update(payload).eq('id',editing):supabase.from('companies').insert({...payload,status:'active'});const {error}=await q;if(error){setMsg(error.message);return}setMsg(editing?'Locadora atualizada com sucesso.':'Locadora cadastrada. Link: '+location.origin+'/l/'+slug);setEditing(null);setForm(empty);await load()}function edit(c:any){setEditing(c.id);setForm({name:c.name||'',slug:c.slug||'',document:c.document||'',email:c.email||'',phone:c.phone||'',plan_name:c.plan_name||'Profissional',monthly_fee:Number(c.monthly_fee||0),primary_color:c.primary_color||'#FFD400',secondary_color:c.secondary_color||'#111111',logo_url:c.logo_url||'',pix_key:c.pix_key||'',efi_payee_code:c.efi_payee_code||'',efi_account_number:c.efi_account_number||'',efi_split_enabled:!!c.efi_split_enabled,notes:c.notes||''});window.scrollTo({top:0,behavior:'smooth'})}async function status(id:string,status:string){if(!supabase)return;const {error}=await supabase.from('companies').update({status,updated_at:new Date().toISOString()}).eq('id',id);setMsg(error?error.message:status==='blocked'?'Locadora bloqueada.':'Locadora ativada.');await load()}async function remove(id:string){if(!supabase||!confirm('Excluir esta locadora? Esta ação pode falhar caso existam reservas vinculadas.'))return;const {error}=await supabase.from('companies').delete().eq('id',id);setMsg(error?'Não foi possível excluir. Prefira bloquear a locadora para preservar o histórico.':'Locadora excluída.');await load()}async function copyLink(c:any){const url=location.origin+'/l/'+(c.slug||c.id);await navigator.clipboard.writeText(url);setMsg('Link copiado: '+url)}if(ok===null)return <main className="container"><div className="card">Carregando...</div></main>;if(!ok)return <main className="container"><div className="card">Acesso exclusivo do Super Admin.</div></main>;return <main className="container"><section className="hero"><h1>Super Admin • Locadoras</h1><p>Cadastre, edite, bloqueie e configure o recebimento Efí de cada locadora.</p></section><form className="card form" onSubmit={save}><h2>{editing?'Editar locadora':'Nova locadora'}</h2><label>Nome<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value,slug:editing?form.slug:slugify(e.target.value)})}/></label><label>Endereço da locadora<input required value={form.slug} onChange={e=>setForm({...form,slug:slugify(e.target.value)})}/><small>/l/{form.slug||'nome-da-locadora'}</small></label><label>CNPJ/CPF<input value={form.document} onChange={e=>setForm({...form,document:e.target.value})}/></label><label>E-mail<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Telefone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Plano<input value={form.plan_name} onChange={e=>setForm({...form,plan_name:e.target.value})}/></label><label>Mensalidade<input type="number" min="0" step="0.01" value={form.monthly_fee} onChange={e=>setForm({...form,monthly_fee:Number(e.target.value)})}/></label><label>Logo (URL)<input value={form.logo_url} onChange={e=>setForm({...form,logo_url:e.target.value})}/></label><div className="cardRow"><label>Cor principal<input type="color" value={form.primary_color} onChange={e=>setForm({...form,primary_color:e.target.value})}/></label><label>Cor secundária<input type="color" value={form.secondary_color} onChange={e=>setForm({...form,secondary_color:e.target.value})}/></label></div><h3>Recebimentos Efí / Split</h3><label>Chave Pix da locadora<input value={form.pix_key} onChange={e=>setForm({...form,pix_key:e.target.value})}/></label><label>Payee Code Efí — usado no cartão<input value={form.efi_payee_code} onChange={e=>setForm({...form,efi_payee_code:e.target.value})}/></label><label>Número da conta Efí — usado no Split Pix<input value={form.efi_account_number} onChange={e=>setForm({...form,efi_account_number:e.target.value})}/></label><label className="paymentOption"><input type="checkbox" checked={form.efi_split_enabled} onChange={e=>setForm({...form,efi_split_enabled:e.target.checked})}/> Ativar split automático para esta locadora</label><small>Ative somente depois de validar os dados da conta Efí da locadora.</small><label>Observações<textarea rows={4} value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><div className="actions"><button className="btn">{editing?'Salvar alterações':'Cadastrar e gerar link'}</button>{editing&&<button type="button" className="btn secondary" onClick={()=>{setEditing(null);setForm(empty)}}>Cancelar</button>}</div>{msg&&<div className="message">{msg}</div>}</form><section className="section card"><h2>Locadoras cadastradas</h2>{list.length===0?<p>Nenhuma locadora cadastrada.</p>:list.map(c=><div className="tenantRow" key={c.id}>{c.logo_url?<img src={c.logo_url} alt={c.name}/>:<div className="tenantLogoFallback">AG</div>}<div className="tenantInfo"><strong>{c.name}</strong><span>{c.document||'Sem documento'} • Plano {c.plan_name||'—'} • {Number(c.monthly_fee||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}/mês</span><span>Split Efí: <strong>{c.efi_split_enabled?'Ativo':'Desativado'}</strong></span><span className={c.status==='blocked'?'statusPill danger':'statusPill'}>{c.status==='blocked'?'Bloqueada':'Ativa'}</span><small>{location.origin}/l/{c.slug||c.id}</small></div><div className="actions"><button className="btn" onClick={()=>copyLink(c)}>Copiar link</button>{c.slug&&<a className="btn secondary" href={'/l/'+c.slug} target="_blank">Abrir</a>}<button className="btn secondary" onClick={()=>edit(c)}>Editar</button>{c.status==='blocked'?<button className="btn" onClick={()=>status(c.id,'active')}>Ativar</button>:<button className="btn secondary" onClick={()=>status(c.id,'blocked')}>Bloquear</button>}<button className="btn secondary" onClick={()=>remove(c.id)}>Excluir</button></div></div>)}</section></main>